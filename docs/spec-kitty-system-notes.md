# Spec Kitty System Notes

This file tracks Spec Kitty tooling observations during ShovelerDB work so they
can be reported upstream if they repeat or block progress.

## 2026-06-01 - Setup and Mission Creation

| Status | Command | Observation | Impact | Workaround | Report Readiness |
| --- | --- | --- | --- | --- | --- |
| Open | `spec-kitty init --here` | The installed CLI is `3.2.0rc31` and rejects `--here` with `No such option: --here`, while the local setup-doctor skill still recommends that flag. | Low. Setup was blocked only until the current syntax was checked. | Used `spec-kitty init --ai codex --non-interactive`, which initializes the current directory when no project name is supplied. | Ready to report as documentation/skill drift if the skill is expected to match `3.2.0rc31`. |
| Watch | `spec-kitty init --ai codex --non-interactive` | After a successful init, the CLI emitted two `direct_ingress_missing_private_team` warnings for `/api/v1/events/batch/`. | Low. Project initialization completed and local artifacts were written. | Treated as non-blocking telemetry/ingress warning. | Report if it repeats during normal local-only mission commands or confuses success detection. |
| Watch | `spec-kitty specify mvp-sql-vector-memory-kernel --mission-type software-dev --json` | The command succeeded and created the mission, then emitted the same `direct_ingress_missing_private_team` warning pair. | Low. Mission artifacts were created successfully. | Continue local workflow and capture recurrence. | Report if local-only usage should suppress these warnings or make them less alarming. |
| Open | `spec-kitty safe-commit --message "Add Spec Kitty mission spec" --to-branch main ...` | The command refused to commit on protected branch `main`, while `setup-plan` simultaneously reports `current_branch: main`, `branch_matches_target: true`, and requires `spec.md` to be committed before planning can continue. | Medium. The documented planning flow cannot advance via `safe-commit` on this project without changing branch strategy or using normal git. | Use a normal git commit for the initial planning artifacts, then continue CLI gates. | Good report candidate if protected-main safe-commit is expected to coexist with planning-on-main missions. |
| Open | `spec-kitty agent mission branch-context --mission mvp-sql-vector-memory-kernel-01KT1MHB --json` | The local `spec-kitty.specify` skill says not to pass `--mission` before mission creation, but implies mission-handle-aware flows after creation. The installed `branch-context` command rejects `--mission`. | Low. We used `check-prerequisites` and `setup-plan` JSON for branch context instead. | Avoid `--mission` on `branch-context` for this CLI version. | Ready to report as command/skill drift if the skill should match `3.2.0rc31`. |
| Open | `spec-kitty agent mission setup-plan --mission mvp-sql-vector-memory-kernel-01KT1MHB --json` | After `plan.md` was made substantive, setup-plan failed during its internal commit step with `safe_commit: refusing to commit to protected branch 'main'` and suggested a coordination worktree path that did not exist. | Medium. Plan phase cannot auto-commit through the documented root-checkout flow on this project. | Commit planning artifacts with normal git and keep running read-only validation where possible. | Strong report candidate; include that `git worktree list` showed only the root checkout. |
| Open | `spec-kitty agent mission finalize-tasks --mission mvp-sql-vector-memory-kernel-01KT1MHB --json` | Validate-only passed and mutating finalization generated `lanes.json`, `status.json`, `acceptance-matrix.json`, and status events, but then failed during safe-commit with the same protected-main refusal. It also emitted repeated `direct_ingress_missing_private_team` warnings before the JSON error. | Medium. Task metadata was produced, but the CLI could not finish its own commit on this branch. | Commit finalized task artifacts with normal git and keep using validate-only/next for state checks. | Strong report candidate alongside the setup-plan safe-commit failure. |
| Watch | `spec-kitty next --agent codex --mission mvp-sql-vector-memory-kernel-01KT1MHB --json` | Query mode succeeded and previewed the `implement` step with WP01 next, but emitted `charter preflight failed (consumer=dashboard): charter_source missing; run spec-kitty charter sync`. | Low. Runtime preview still worked and no mission guard failed. | Treat as non-blocking until the project has a charter, or run charter sync if dashboard/charter workflow becomes necessary. | Report only if query-mode runtime should not warn for projects that have not opted into charter setup. |
| Open | `spec-kitty agent action implement WP01 --mission mvp-sql-vector-memory-kernel-01KT1MHB --agent codex:gpt-5:implementer-ivan:implementer` | Implementation workspace allocation failed with `charter_source missing; run spec-kitty charter sync`, turning the earlier query-mode charter warning into a hard blocker. `charter sync` alone also failed because no `.kittify/charter/charter.md` existed. | High for first implementation. The mission could not enter the WP implementation loop until a runtime charter existed. | Bootstrapped a minimal software-dev charter with `spec-kitty charter interview --defaults`, generated it, and confirmed charter status fresh. | Strong report candidate: implement preflight remediation suggests `charter sync`, but a no-charter repo first needs interview/generate or a clearer bootstrap command. |
| Open | `spec-kitty agent action implement WP01 --mission mvp-sql-vector-memory-kernel-01KT1MHB --agent codex:gpt-5:implementer-ivan:implementer` | After runtime charter generation, implementation workspace allocation failed again with `synthesized_drg missing; run spec-kitty charter synthesize`. | High for first implementation. A fresh project requires both runtime charter and minimal doctrine seed before any WP can start. | Ran `spec-kitty charter synthesize`, which used the documented fresh-project seed path and produced `.kittify/doctrine/PROVENANCE.md` plus `.kittify/charter/synthesis-manifest.yaml`. | Report if implement preflight should offer the full bootstrap sequence rather than one missing prerequisite at a time. |
| Open | `spec-kitty doctrine list --kind directive` | The charter-doctrine skill references `spec-kitty doctrine list`, but this CLI reports `No such command 'list'`. | Low. We did not need manual doctrine generation because fresh-project synthesis worked. | Avoid `doctrine list` on this CLI version; use `charter synthesize --dry-run`/`charter synthesize` for minimal seed path. | Ready to report as skill/CLI drift if the skill is expected to target `3.2.0rc31`. |
| Open | `spec-kitty agent action implement WP01 --mission mvp-sql-vector-memory-kernel-01KT1MHB --agent codex:gpt-5:implementer-ivan:implementer` | After charter and doctrine preflight passed, canonical implementation action refused to mutate status on protected branch `main` and suggested `--no-auto-commit`, but this command's help does not expose that flag. | High for canonical implementation flow. We could not use the documented `agent action implement` path to allocate WP01. | Used internal `spec-kitty implement WP01 --mission ... --no-auto-commit --json`, then manually committed lane code and status artifacts. | Strong report candidate: canonical command suggests a flag it does not accept, while the internal command accepts it. |
| Watch | `spec-kitty agent tasks mark-status ...` and `spec-kitty agent tasks move-task ...` | Status transitions on `main` also refused auto-commit and required `--no-auto-commit`; with that flag they succeeded but emitted the repeated `direct_ingress_missing_private_team` warnings. | Medium. Manual status artifact commits are required while `main` is protected. | Use `--no-auto-commit`, inspect changed mission files, and commit status updates manually. | Report with the protected-main safe-commit family if this project shape is expected to work without a coordination worktree. |
| Watch | `git commit` in a lane after `spec-kitty agent action review ...` | The commit succeeded but emitted `[spec-kitty guard] WARNING: ACTIVE_WP_CONTEXT_AMBIGUOUS` because the WP was already in `in_review`; observed on WP01 lane-a and WP03 lane-c. The review action also left an untracked `.spec-kitty/review-lock.json` in the lane worktree. | Low. Commits and tests succeeded, but the working tree needed `.spec-kitty/` ignored to stay clean. | Added `.spec-kitty/` to `.gitignore` and continued review. | Report if review locks are expected to be auto-ignored by project init or if in-review lane commits should resolve to the active WP. |
| Open | `spec-kitty agent tasks status --mission mvp-sql-vector-memory-kernel-01KT1MHB` after approving WP01 | The pretty status view reported `Total WPs: 1` and showed WP01 still in review, while `status.json` and `spec-kitty next --json` correctly reported 8 WPs, WP01 approved, and WP02 next. | Medium. The human-readable status command is misleading during orchestration even though the machine-readable scheduler appears correct. | Trust `spec-kitty next --json` and the materialized `status.json`; record the mismatch for upstream diagnosis. | Strong report candidate because it can cause an orchestrator to stop or re-review an already approved WP. |
| Open | `spec-kitty agent profile show implementer-ivan` | The ad-hoc-profile-load skill documents `agent profile show`, but this CLI only exposes `agent profile list`; `show` fails with `No such command 'show'`. | Low. The profile ID was still visible through `agent profile list`, and action-scoped charter context loaded successfully. | Use `spec-kitty agent profile list` plus `spec-kitty charter context --action implement --json` for current CLI behavior. | Ready to report as skill/CLI drift for profile loading guidance. |
| Open | `spec-kitty implement WP03 --mission mvp-sql-vector-memory-kernel-01KT1MHB --no-auto-commit --json` after approving WP02 | WP03 correctly depended on WP02 in task metadata, but the created `lane-c` worktree was based on the mission branch and did not contain approved WP02 files (`src/db/value.zig`, `src/db/catalog.zig`). | High for dependency-based implementation. A dependent WP can start without the code it is supposed to build on, causing compile failures or duplicate reimplementation. | Cherry-pick the approved WP02 implementation commit into the WP03 lane before coding, and record the dependency-base mismatch. | Strong report candidate for dependency-aware workspace creation. |
