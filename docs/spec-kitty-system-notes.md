# Spec Kitty System Notes

This file tracks Spec Kitty tooling observations during ShovelerDB work so they
can be reported upstream if they repeat or block progress.

## 2026-06-01 - Setup and Mission Creation

| Status | Command | Observation | Impact | Workaround | Report Readiness |
| --- | --- | --- | --- | --- | --- |
| Open | `spec-kitty init --here` | The installed CLI is `3.2.0rc31` and rejects `--here` with `No such option: --here`, while the local setup-doctor skill still recommends that flag. | Low. Setup was blocked only until the current syntax was checked. | Used `spec-kitty init --ai codex --non-interactive`, which initializes the current directory when no project name is supplied. | Ready to report as documentation/skill drift if the skill is expected to match `3.2.0rc31`. |
| Watch | `spec-kitty init --ai codex --non-interactive` | After a successful init, the CLI emitted two `direct_ingress_missing_private_team` warnings for `/api/v1/events/batch/`. | Low. Project initialization completed and local artifacts were written. | Treated as non-blocking telemetry/ingress warning. | Report if it repeats during normal local-only mission commands or confuses success detection. |
| Watch | `spec-kitty specify mvp-sql-vector-memory-kernel --mission-type software-dev --json` | The command succeeded and created the mission, then emitted the same `direct_ingress_missing_private_team` warning pair. | Low. Mission artifacts were created successfully. | Continue local workflow and capture recurrence. | Report if local-only usage should suppress these warnings or make them less alarming. |
| Open | `spec-kitty safe-commit --message "Add Spec Kitty mission spec" --to-branch main ...` | The command refused to commit on protected branch `main`, while `setup-plan` simultaneously reports `current_branch: main`, `branch_matches_target: true`, and requires `spec.md` to be committed before planning can continue. | Medium. The documented planning flow cannot advance via `safe-commit` on this project without changing branch strategy or using normal git. | Use a normal git commit for the initial planning artifacts, then continue CLI gates. | Good report candidate if protected-main safe-commit is expected to coexist with planning-on-main missions. |
| Open | `spec-kitty agent mission branch-context --mission mvp-sql-vector-memory-kernel-01KT1MHB --json` | The local `spec-kitty.specify` skill says not to pass `--mission` before mission creation, but implies mission-handle-aware flows after creation. The installed `branch-context` command rejects `--mission`. | Low. We used `check-prerequisites` and `setup-plan` JSON for branch context instead. | Avoid `--mission` on `branch-context` for this CLI version. | Ready to report as command/skill drift if the skill should match `3.2.0rc31`. |
| Open | `spec-kitty agent mission setup-plan --mission mvp-sql-vector-memory-kernel-01KT1MHB --json` | After `plan.md` was made substantive, setup-plan failed during its internal commit step with `safe_commit: refusing to commit to protected branch 'main'` and suggested a coordination worktree path that did not exist. | Medium. Plan phase cannot auto-commit through the documented root-checkout flow on this project. | Commit planning artifacts with normal git and keep running read-only validation where possible. | Strong report candidate; include that `git worktree list` showed only the root checkout. |
| Open | `spec-kitty agent mission finalize-tasks --mission mvp-sql-vector-memory-kernel-01KT1MHB --json` | Validate-only passed and mutating finalization generated `lanes.json`, `status.json`, `acceptance-matrix.json`, and status events, but then failed during safe-commit with the same protected-main refusal. It also emitted repeated `direct_ingress_missing_private_team` warnings before the JSON error. | Medium. Task metadata was produced, but the CLI could not finish its own commit on this branch. | Commit finalized task artifacts with normal git and keep using validate-only/next for state checks. | Strong report candidate alongside the setup-plan safe-commit failure. |
| Watch | `spec-kitty next --agent codex --mission mvp-sql-vector-memory-kernel-01KT1MHB --json` | Query mode succeeded and previewed the `implement` step with WP01 next, but emitted `charter preflight failed (consumer=dashboard): charter_source missing; run spec-kitty charter sync`. | Low. Runtime preview still worked and no mission guard failed. | Treat as non-blocking until the project has a charter, or run charter sync if dashboard/charter workflow becomes necessary. | Report only if query-mode runtime should not warn for projects that have not opted into charter setup. |
| Open | `spec-kitty agent action implement WP01 --mission mvp-sql-vector-memory-kernel-01KT1MHB --agent codex:gpt-5:implementer-ivan:implementer` | Implementation workspace allocation failed with `charter_source missing; run spec-kitty charter sync`, turning the earlier query-mode charter warning into a hard blocker. `charter sync` alone also failed because no `.kittify/charter/charter.md` existed. | High for first implementation. The mission could not enter the WP implementation loop until a runtime charter existed. | Bootstrapped a minimal software-dev charter with `spec-kitty charter interview --defaults`, generated it, and confirmed charter status fresh. | Strong report candidate: implement preflight remediation suggests `charter sync`, but a no-charter repo first needs interview/generate or a clearer bootstrap command. |
| Open | `spec-kitty agent action implement WP01 --mission mvp-sql-vector-memory-kernel-01KT1MHB --agent codex:gpt-5:implementer-ivan:implementer` | After runtime charter generation, implementation workspace allocation failed again with `synthesized_drg missing; run spec-kitty charter synthesize`. | High for first implementation. A fresh project requires both runtime charter and minimal doctrine seed before any WP can start. | Ran `spec-kitty charter synthesize`, which used the documented fresh-project seed path and produced `.kittify/doctrine/PROVENANCE.md` plus `.kittify/charter/synthesis-manifest.yaml`. | Report if implement preflight should offer the full bootstrap sequence rather than one missing prerequisite at a time. |
| Open | `spec-kitty doctrine list --kind directive` | The charter-doctrine skill references `spec-kitty doctrine list`, but this CLI reports `No such command 'list'`. | Low. We did not need manual doctrine generation because fresh-project synthesis worked. | Avoid `doctrine list` on this CLI version; use `charter synthesize --dry-run`/`charter synthesize` for minimal seed path. | Ready to report as skill/CLI drift if the skill is expected to target `3.2.0rc31`. |
| Open | `spec-kitty agent action implement WP01 --mission mvp-sql-vector-memory-kernel-01KT1MHB --agent codex:gpt-5:implementer-ivan:implementer` | After charter and doctrine preflight passed, canonical implementation action refused to mutate status on protected branch `main` and suggested `--no-auto-commit`, but this command's help does not expose that flag. | High for canonical implementation flow. We could not use the documented `agent action implement` path to allocate WP01. | Used internal `spec-kitty implement WP01 --mission ... --no-auto-commit --json`, then manually committed lane code and status artifacts. | Strong report candidate: canonical command suggests a flag it does not accept, while the internal command accepts it. |
| Watch | `spec-kitty agent tasks mark-status ...` and `spec-kitty agent tasks move-task ...` | Status transitions on `main` also refused auto-commit and required `--no-auto-commit`; with that flag they succeeded but emitted the repeated `direct_ingress_missing_private_team` warnings. Observed again while marking WP07 T017-T019 done and moving WP07 to review. | Medium. Manual status artifact commits are required while `main` is protected. | Use `--no-auto-commit`, inspect changed mission files, and commit status updates manually. | Report with the protected-main safe-commit family if this project shape is expected to work without a coordination worktree. |
| Watch | `git commit` in a lane after `spec-kitty agent action review ...` | The commit succeeded but emitted `[spec-kitty guard] WARNING: ACTIVE_WP_CONTEXT_AMBIGUOUS` because the WP was already in `in_review`; observed on WP01 lane-a and WP03 lane-c. The review action also left an untracked `.spec-kitty/review-lock.json` in the lane worktree. | Low. Commits and tests succeeded, but the working tree needed `.spec-kitty/` ignored to stay clean. | Added `.spec-kitty/` to `.gitignore` and continued review. | Report if review locks are expected to be auto-ignored by project init or if in-review lane commits should resolve to the active WP. |
| Open | `spec-kitty agent tasks status --mission ...` after lane transitions | The pretty status view reported `Total WPs: 1` and showed WP01 still in review after approving WP01 in the MVP mission, while `status.json` and `spec-kitty next --json` correctly reported 8 WPs, WP01 approved, and WP02 next. In the MySQL syntax mission after moving WP07 to review, the board showed zero in-progress WPs but the summary still reported `In Progress: 1`. | Medium. The human-readable status command is misleading during orchestration even though the machine-readable scheduler appears correct. | Trust `spec-kitty next --json` and the materialized `status.json`; record mismatches for upstream diagnosis. | Strong report candidate because it can cause an orchestrator to stop, re-review, or mis-sequence work. |
| Open | `spec-kitty agent profile show implementer-ivan` | The ad-hoc-profile-load skill documents `agent profile show`, but this CLI only exposes `agent profile list`; `show` fails with `No such command 'show'`. | Low. The profile ID was still visible through `agent profile list`, and action-scoped charter context loaded successfully. | Use `spec-kitty agent profile list` plus `spec-kitty charter context --action implement --json` for current CLI behavior. | Ready to report as skill/CLI drift for profile loading guidance. |
| Open | `spec-kitty implement WP03/WP04/WP06/WP07/WP08 --mission mvp-sql-vector-memory-kernel-01KT1MHB --no-auto-commit --json` after dependency approval | Dependent WPs were based on the mission branch without approved dependency files: WP03 lacked WP02, WP04 lacked WP01, WP02, and WP03, WP06 lacked WP02 and WP03, WP07 lacked WP01 through WP06, and WP08 lacked WP01 through WP07 plus current main task/docs context. | High for dependency-based implementation. A dependent WP can start without the code it is supposed to build on, causing compile failures, duplicate reimplementation, or missing current task artifacts. | Cherry-pick approved dependency commits into the dependent lane before coding, and read current main task/docs context from the root checkout when the lane lacks it. | Strong report candidate for dependency-aware workspace creation. |
| Watch | `git merge --no-edit main` inside WP08 lane after dependency cherry-picks | Attempting to bring current main context into the stale lane produced add/add conflicts in Spec Kitty status/task artifacts such as `status.json`, `status.events.jsonl`, and WP task files. | Medium. A normal context merge was noisy enough to risk status artifact damage during final acceptance. | Aborted the merge, kept dependency code cherry-picks, and updated only WP08-owned files directly. | Report with the dependency-base issue if dependent lanes are expected to merge cleanly with root status history. |
| Watch | WP04 lane commit containing live executor wiring | Spec Kitty guard emitted `ACTIVE_WP_SCOPE_VIOLATION` because `build.zig`, `src/db/executor.zig`, `src/db/view.zig`, and `src/lib.zig` were outside WP04 owned files. Those edits were needed to compile, expose, and acceptance-test the row-source executor through live SELECT/view paths instead of leaving the new module unreachable until a later WP. | Medium. Strict file ownership conflicts with the implement-review requirement that new code be reachable from live entry points before handoff. | Kept the minimal wiring changes in the lane and logged the guard warning for review. | Report if ownership metadata should include integration touchpoints or if the dead-code check should understand deferred WP08 wiring. |
| Watch | WP05 lane commit containing aggregate parser/executor wiring | Spec Kitty guard emitted `ACTIVE_WP_SCOPE_VIOLATION` because `build.zig`, `src/cli/benchmark.zig`, `src/db/executor.zig`, `src/db/query_source.zig`, `src/db/view.zig`, `src/lib.zig`, `src/sql/ast.zig`, and `src/sql/parser.zig` were outside WP05 owned files. The WP owned only `src/db/aggregate.zig` and its integration test, but aggregates cannot be live without AST/parser, row-source executor, diagnostics, view cloning, benchmark, and build wiring. | Medium. The owned-files model remains too narrow for feature WPs whose correctness depends on live integration paths. | Kept the integration edits, validated with `zig build test`, CLI aggregate smoke, benchmark smoke, and diff check, and logged the warning. | Report with WP04 as evidence that integration touchpoints need first-class ownership metadata. |
| Open | `spec-kitty status --help` | The installed CLI has no top-level `status` command. | Low. A human naturally reaches for this while checking mission state, but it fails with `No such command 'status'`. | Use `status.json`, `status.events.jsonl`, `lanes.json`, and `spec-kitty next --json` as the truth surfaces. | Report as an ergonomics/docs gap if a top-level status surface is expected. |
| Watch | `spec-kitty specify mysql-style-syntax-completion --mission-type software-dev --json` | The command succeeded but created an empty `spec.md` and repeated direct-ingress warnings. | Low. The mission existed, but the spec artifact had to be authored manually from scratch. | Treat created files as scaffolding and inspect before assuming content exists. | Report if `specify` is expected to populate a non-empty template for software-dev missions. |
| Watch | `spec-kitty tasks --mission mysql-style-syntax-completion-01KT2G5Z --json` | Task finalization first failed ownership validation because feature-oriented WPs shared parser/executor/docs paths. After removing overlaps, it failed again because `authoritative_surface` must be a prefix of at least one `owned_files` entry. | Medium. Larger milestone WPs need path-shaped ownership metadata even when the natural work is feature-shaped and crosses parser/executor boundaries. | Recast WP owned files as unique files/future modules and use concrete path prefixes for `authoritative_surface`; document required integration touchpoints in the WP body. | Report if Spec Kitty should support feature WPs with declared integration touchpoints or better validation guidance. |
| Open | `spec-kitty tasks --mission mysql-style-syntax-completion-01KT2G5Z --json` after ownership fixes | Finalization generated `lanes.json`, `status.json`, `acceptance-matrix.json`, and status events, then failed with `safe_commit: refusing to commit to protected branch 'main'` and suggested a coordination worktree path. | Medium. The useful metadata is produced, but the command exits non-zero and still requires manual commit on this project shape. | Keep generated artifacts, use `--no-auto-commit` for later status moves, inspect status files, and commit manually with normal git. | Add to the existing protected-main safe-commit report family. |
| Open | `spec-kitty implement WP03/WP04/WP05/WP06/WP07 --mission mysql-style-syntax-completion-01KT2G5Z --base main --no-auto-commit --json` | The command reported a current `base_commit` in root WP metadata, but created lane worktrees checked out at older commit `a50204e` or the stale coordination branch and lacked the new mission files; repeated for WP03 lane-c, WP04 lane-d, WP05 lane-e, WP06 lane-f, and WP07 lane-g. | High for delegated implementation. A worker launched immediately in those lanes would not see the current mission, WP prompts, or the latest code changes. | Manually ran `git merge --no-edit main` inside each new lane worktree, which fast-forwarded or merged to the real current main before dispatch. | Strong report candidate: explicit `--base main` should create the lane from that commit or fail if it cannot. |
| Watch | WP06 lane commit containing parser, executor, persistence, and build wiring | Spec Kitty guard emitted `ACTIVE_WP_SCOPE_VIOLATION` because `build.zig`, `src/db/executor.zig`, `src/db/persistence.zig`, `src/lib.zig`, `src/sql/ast.zig`, and `src/sql/parser.zig` were outside WP06 owned files. The WP owned catalog, DDL helper, and the integration test, but DDL metadata cannot be live or durable without parser, executor, snapshot, export, and build integration. | Medium. The ownership model again conflicts with live-entry-point implementation requirements. | Kept the minimal integration edits, validated with `zig build test`, diff-scoped `zig fmt --check`, CLI smoke, and logged the warning. | Report with WP04/WP05 as further evidence that feature WPs need declared integration touchpoints. |
| Watch | `spec-kitty agent tasks move-task WP06 --mission mysql-style-syntax-completion-01KT2G5Z --to for_review --no-auto-commit ...` | The lane WP prompt frontmatter listed `subtasks: []`, but move-task blocked on unchecked T015 and T016 from the planning repo's `tasks.md`, and printed `worktree copy ignored`. | Low to medium. The correct gate was discoverable, but a delegated worker reading only the prompt would not know which subtask IDs must be marked done. | Inspected root `tasks.md`, marked T015/T016 done with `--no-auto-commit`, then moved WP06 to `for_review`. | Report if prompts are expected to include task IDs required by move-task gates. |
| Open | `spec-kitty agent action review WP06/WP07 --mission mysql-style-syntax-completion-01KT2G5Z --agent codex:gpt-5:implementer-ivan:reviewer` | Review allocation attempted to prepare the coordination worktree from stale commit `a50204e`/stale coordination state, then failed with `LANE_AUTO_REBASE_FAILED` on `status.events.jsonl`, saying the file was marked conflicted but contained no conflict markers. WP06 and WP07 both left transient uncommitted WP task-file edits in root. | Medium. The canonical review action could not be used despite clean implementation lanes and committed for-review status. | Verified lanes remained clean, removed transient root edits, reviewed the lane diffs manually, and used manual move-task approval where appropriate. | Strong report candidate with the stale-base and status-artifact conflict family. |
| Watch | `spec-kitty agent tasks move-task WP06 --mission mysql-style-syntax-completion-01KT2G5Z --to done --no-auto-commit ...` after cherry-picking lane-f to main | After main validation, done transition first required lane-f to rebase onto main. That rebase skipped the already-applied lane commit, after which move-task refused because the lane had no commits beyond main. | Low to medium. The correct state was landed and validated, but the lane-based done gate did not recognize the cherry-picked implementation without force. | Used `--force` with a note that lane-f was empty because its patch had already landed on main as `ed79ba4`. | Report with the manual landing/cherry-pick workflow if per-WP done transitions are expected to support already-landed lane commits. |
| Watch | `git rebase main` inside WP07 lane after marking T017-T019 done on main | The rebase attempted to replay stale WP06 status commits and hit add/add conflicts in `status.events.jsonl`, `status.json`, `tasks.md`, and `tasks/WP06-mysql-ddl-index-metadata.md`, including `.merge_file_*: line 1 is missing a valid at timestamp`. | Medium. The required "lane is behind main" remediation can pull in stale coordination history instead of just aligning the implementation commit. | Aborted the plain rebase and used `git rebase --onto main fe8148c`, replaying only the WP07 implementation commit onto current main. | Report with the stale-base/status-artifact conflict family. |
